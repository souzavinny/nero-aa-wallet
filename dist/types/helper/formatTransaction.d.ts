import { CombinedTransaction, FormattedTransaction } from '@/types';
export declare const formatTransaction: (tx: CombinedTransaction, userAddress: string, explorerUrl: string) => FormattedTransaction;
